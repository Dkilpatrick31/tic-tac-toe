// Upon loading the game, my functions will be enabled.
// document.onload = function(){

 // this function will tell me that at the start of the game, player 1('X') is first.
//   function startGame() {
//    document.turn = "X";
//    console.log(document.turn);
//    setMessage(document.turn + " is first.")
//   }

// this function will set the message of the class message.
//  function setMessage(msg) {
//    var element = document.getElementById("message");
//    element.innerHTML(msg);
//   }

// this function will tell me to click another square when trying to choose a square that has already be chosen.
//  function nextMove(square) {
//    if(square.innerText === '') {
//      square.innerText = document.turn;
//      switchTurn()
//    } else {
//      setMessage("Choose another square")
//    }

// this function will rotate between turns, making it to whenever I choose "X" the next turn will be "O."
//  function switchTurn() {
//    if(document.turn === "X") {
//      document.turn = "O";
//    } else {
//      document.turn = "X";
//    }
//  }
//  });
// }
